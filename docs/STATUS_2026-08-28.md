# Fight AI — Cierre de jornada 2026-08-28

## Estado Android

Baseline de trabajo: **Fight AI 0.14.x** en la rama `qa/cloud-android`.

### Completado y verificado

- Source 0.14 reconstruido correctamente dentro de GitHub Actions.
- Validación estática del source: PASS.
- Contratos de producto 0.14: PASS.
- Generación/validación de demos de movimiento: PASS.
- Build real de Android APK: PASS.
- APK instalado correctamente en emulador Android 35: PASS.
- Gate de configuración de Gemini: PASS.
- Prueba autenticada de video con Gemini: PASS.
- Objetivo de la prueba Gemini: peleador de **guantes rojos**.
- Gemini devolvió evidencia aceptada y `usedInReport=true`.
- Artefacto de prueba Gemini almacenado por GitHub Actions.
- Reporte PDF forma parte del baseline 0.14.
- Estado de IA distingue motor CV/Pose de Gemini usado realmente en el reporte.
- Workflow de QA Android, agente virtual y evidencia de regresión están versionados en GitHub.

### Bloqueo actual del agente Android

El APK compila e instala. El bloqueo pendiente no corresponde al build de Fight AI.

En la ejecución Android 35, el emulador cloud presentó:

- `ANR in com.android.phone`
- presión de CPU cercana al 100 %
- después del retry, el agente no encontró a tiempo el texto esperado de Fight AI en el UI dump.

Por lo tanto, el gate final `real-app-build` todavía no queda verde. El APK y los logs/screenshots del intento quedaron preservados para continuar la depuración.

### Próxima iteración Android

1. Endurecer el agente contra ANR del System UI / `com.android.phone`.
2. Esperar estado real de Activity/Fight AI en vez de depender solo del texto de onboarding.
3. Capturar activity, hierarchy, screenshot y logcat por intento.
4. Ejecutar navegación completa del agente virtual.
5. Probar flujo completo de análisis y PDF.
6. Mejorar el PDF/Visual Coach con footwork, líneas de ataque, pivotes, salidas, slips, flechas y trayectorias ligadas a cada corrección.
7. Ejecutar nuevamente la regresión consolidada antes de declarar una nueva beta.
8. Cerrar Android solo cuando el gate conjunto quede verde.

## Gemini

La prueba autenticada del 28-08-2026 ejecutó Gemini sobre un clip real de sparring enfocado en el peleador de guantes rojos.

Contrato validado:

- provider = Gemini
- target = red gloves
- timestamped evidence = presente
- usedInReport = true

La UI y los reportes deben seguir mostrando `+ GEMINI` únicamente cuando Gemini haya contribuido con evidencia aceptada.

## PDF

Estado actual:

- generación de PDF: implementada
- descarga/compartir: integrada al baseline 0.14
- contenido técnico/táctico: integrado
- siguiente mejora prioritaria: láminas visuales con flechas, trayectorias y posiciones inicial/final ligadas a cada corrección

## Estado web al cierre

La versión web **sí está en desarrollo y desplegada** en la rama `web/mvp`.

Estado confirmado:

- Next.js 15.5.24 + React 19 + TypeScript.
- Upload y reproducción local de video.
- Selección básica del peleador objetivo.
- Boxing/kickboxing + stance.
- Timestamps clickeables.
- Reporte con fortalezas, prioridades, rival, plan táctico y drills.
- Estado explícito de provider + `usedInReport`.
- Fallback Gemini server-side.
- CI con typecheck, build, runtime smoke y Docker build.
- Deploy AWS mediante GitHub OIDC.
- ECR privado + ECS Fargate + ALB.
- Endpoint público de beta con `/api/health`.
- Gemini configurado del lado servidor.
- Arquitectura y contrato compartido documentados en `docs/GRAPIFY_BETA_SPEC.md`.

Pendientes web principales:

1. migrar `web/mvp` a un repositorio web independiente;
2. completar paridad real con Android;
3. flujo asíncrono/privado para videos grandes;
4. HTTPS;
5. backend compartido CV/Pose;
6. logging/observabilidad;
7. E2E real de video grande + reporte;
8. mover el secreto Gemini a un servicio AWS administrado cuando la cuenta lo permita.

## Repositorios / ramas al cierre

Repositorio actual: `pinoaraj/fight-ai`

- `main`: baseline común.
- `qa/cloud-android`: Android 0.14.x + QA.
- `web/mvp`: web MVP + AWS deployment.

Objetivo de organización: mantener Android en `fight-ai` y mover la versión web a un repositorio separado `pinoaraj/fight-ai-web`.

## Punto exacto para retomar mañana

**Android:** resolver el gate del agente real en emulador y ejecutar la regresión consolidada.

**Web:** completar la separación a `fight-ai-web`, reconfigurar GitHub OIDC/deploy para el nuevo repository subject y continuar desde el commit migrado sin perder el estado actual.

## Estado de cierre

Android: **BUILD OK / GEMINI OK / SOURCE QA OK / EMULATOR AGENT PENDING**

Web: **MVP IMPLEMENTADO / AWS DEPLOY ACTIVO / GEMINI RUNTIME CONFIGURADO / PARIDAD ANDROID + HARDENING PENDIENTES**
